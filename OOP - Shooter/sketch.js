// OOP - Shooter

class Bullet {
  constructor(playerX, playerY) {
    this.x = x;
    this.y = y;
    this.color = "red";
    this.alpha = 255;

  }

  updater(){
    alpha()--;
  }

  display(){

  }

  isDead(){
    if (alpha <= 0);
      splice
  }
}

theBullets = [];

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}
